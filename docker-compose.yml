version: "3"

services:
  web:
    build: .
    ports:
      - "${PORT}:5000"
    volumes:
      - db-data:/data         # Named volume → for SQLite DB file
      - ./app/templates:/app/templates  # Bind mount → live HTML editing

volumes:
  db-data:
